/**
 * Server-side rendering entry point.
 *
 * SSR is optional for this application. To enable:
 * 1. Add @inertiajs/react server adapter
 * 2. Configure vite.config.js with ssr: 'resources/js/ssr.tsx'
 * 3. Run `php artisan inertia:start-ssr`
 *
 * For now, the app uses client-side rendering only.
 */

export {};
